return {
    'tpope/vim-surround',
    version = "*",
    lazy = false,
    dependencies = {
    },
    config = function()
        -- [[ surround ]]
        -- cs"'
        -- cs"<q>
        -- ds"
        -- ysiw]
        -- cs]{
        -- yss)
        -- ds{ds(
        -- ysiw)
    end,
}
